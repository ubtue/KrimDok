<? $this->headTitle($this->translate('Help')); ?>
<? if ($help = $this->helpText()->render("faq")): ?>
  <? foreach ($this->helpText()->getWarnings() as $warning): ?>
    <p class="warning"><?=$this->transEsc($warning)?></p>
  <? endforeach; ?>
  <?=$help?>
<? else: ?>
  <p class="warning"><?=$this->transEsc('faq_page_missing')?></p>
<? endif; ?>